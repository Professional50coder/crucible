# Third-party repos cloned for study only — never shipped, never copied wholesale.
# See docs/PRIOR_ART.md for what was learned and how each source is attributed.
reference/

# Local editor / tooling configuration. Not part of the project.
.claude/

# Working log of mistakes and their lessons. Useful to us, not a document for
# judges — the corrections themselves are public in CHANGELOG.md.
MISTAKES.md

# Our own working boards: what is done, what is left, what is blocked. These are
# for running the project, not for reading it. The public record of progress is
# the commit history and CHANGELOG.md.
PLAN.md
TODO.md

node_modules/
dist/
.next/
.vercel/
coverage/
.env
.env.*
!.env.example
*.log
.DS_Store

# Secrets — contains a plaintext private key. NEVER commit.
CREDENTIALS.local.md
MAINNET_WALLET.local.md
*.local.md
secrets/
*.pem
*.key
*.keystore
mnemonic*
wallet-*.json

# Fine-tuning artifacts. The retrieved model stays on disk and is referenced by
# hash from the docs; 89 MB of encrypted weights does not belong in git.
runs/**/*.bin
runs/ack-*/
models/
*.safetensors
encrypted_model.bin
model_output.zip
lora_adapter/

# Hardhat / Foundry
artifacts/
cache/
out/
typechain-types/
broadcast/
